PHP version
    Performance in time used per transaction

Roadmap:
Better install instructions
    Dependencies
    Sizing estimates
Direct mode
    Logfile maintenance
    Rotation
    Zip

Perl version

Completed:
    Parse through POST body
    Convert epoch time to local time--Should keep epoch time in log line
    Authentication secret--is set via header X-RTR-Secret
    Call syslog function
    Multiple matches per RTR line
    Make syslog string
    Add debug output--started, nowhere done
    Input validation for each log line element 
    Direct mode
        Threadsafe writing
    